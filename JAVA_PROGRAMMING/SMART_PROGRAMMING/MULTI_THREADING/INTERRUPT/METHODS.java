import java.util.*;

// 1. -> interrupted() is used to check wether a thread is interrupted or not.
//    -> isInterrupted() is used to check wether a thread is interrupted or not.
// 2. -> interrupted() method clears the interrupted status from true to false if thread is interrupted.
//    -> isInterrupted() method does not clear the interrupted status.
// 3. -> interrupted() method will change the result if called twice.
//    -> isInterrupt() method will produce same result if called twice.
// 4. -> Syntax:- public static boolean interrupted(){-}
//    ->          public boolean isInterrupted(){-}


public class METHODS{
    public static void main(String[] args) {
        
    }
}